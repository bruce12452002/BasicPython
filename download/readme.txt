有很多非 python 提供的包，可以提升開發的效率，以下是常用的包
numpy 計算
pandas  資料分析
pyspark apache-flink 大數據計算
matplotlib pyecharts 圖形
tensorflow 人工智能

如想用 numpy，可用內鍵的 pip 指令下，pip install numpy，網路慢可用「 -i 網址」
或者用圖示 2.png 的「＋」號，搜尋並下載，網路慢可在右側 Options 用「 -i 網址」，下載好後，左側的　External Libraries 就能看到了